import PostIndex from "@/components/post-index";

export default function ProjectsPage() {
    return (
        <PostIndex
            type="projects"
            title="projects"
            intro="Things I have built, in various states of finish."
        />
    );
}
